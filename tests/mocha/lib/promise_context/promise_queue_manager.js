"use strict";

// Load external packages
const Chai        = require('chai')
    , assert      = Chai.assert
    , rootPrefix  = "../../../.."
    , Core        = require( rootPrefix + "/index" )
    , Logger      = Core.Logger
;